# Optional: S3 remote state. Copy to backend.hcl and run: terraform init -backend-config=backend.hcl
# Replace ACCOUNT_ID with: aws sts get-caller-identity --query Account --output text
# Terraform 1.9+: use_lockfile uses S3-native locking (no DynamoDB table needed).

bucket       = "mongotf-terraform-state-ACCOUNT_ID"
key          = "mongotf/terraform.tfstate"
region       = "eu-central-1"
use_lockfile = true
